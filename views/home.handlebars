<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h1 class="title">{{title}}</h1>
        </div>
    </div>
</div>
<div class="container-fluid">
    <div class="row">
        <div class="col-2">
            <ul>
                {{#each cities}}
                <li class=" listRegions list-{{this}}">{{this}}</li>
                {{/each}}
            </ul>
        </div>
        <div class="col-8">
            {{>carte}}
        </div>
    </div>
</div>
